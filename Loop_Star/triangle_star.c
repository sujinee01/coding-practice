#include <stdio.h>

int main(void)
{
    int i = 0;
    
    for (; i < 6; i++) {
        for(int j; j=<i/2; j++)
            printf(" ");
            if (j>i/2)
                for(;j=<i;j++) {
                    printf("*")
            else (
                for(int k; k=<i/2; k++)
                printf(" ");
            )
            }
    }

    return 0;
}